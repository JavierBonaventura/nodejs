function multiplicar(num1, num2){
    let resultado = num1 * num2
    if (num1 === 0 || num2 === 0) 
    {
        return 0
    }
   else {
       return resultado
    }    
}

module.exports = multiplicar